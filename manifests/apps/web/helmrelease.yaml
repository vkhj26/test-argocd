apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-valeria
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://charts.bitnami.com/bitnami'
    targetRevision: 16.0.0
    chart: nginx
    helm:
      values: |
        image:
          tag: latest
        staticSiteConfig:
          enabled: true
          content: "<h1>¡Hola Valeria!</h1><p>¡Lograste configurar GitOps!</p>"
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default